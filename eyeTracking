import os
import numpy as np
import cv2
import math

"""
General Process:
1. Initialize variables for eyes, irises,pupils, head movement, face; bools for if those vars are detected 
    if needed, also add in functions to simply draw/outline those facial features
2. detect the primary face by size, so closest face is the one to track
3. Track irises and head movement together

"""